package game;

/**
	Bloops: bloques con personalidad (estados, skins, squash & stretch).
	Reservado para la capa de presentación sobre los ids de celda del `Grid`.
**/
class Bloop {
	public function new() {}
}
